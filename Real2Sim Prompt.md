---
name: real2sim-prompt
description: Reconstruct robot-object scenes and actions in Blender from multiview real video, then transfer them to MuJoCo and calibrate physical interactions; validate keyframes and export frame-matched comparisons.
metadata:
  version: "v1"
---

# Real2Sim Prompt v1

分两步处理 Real2Sim：**第一步，真实视频 → Blender 场景与动作；第二步，Blender → MuJoCo 场景与实际物理执行。** 两步共用源索引、交互关键帧、坐标与参数来源记录。默认追求**可用的交互重建**；先让目标、相机方向、相对位置和操作过程可辨认，再按任务需要提高精度。用户认可的质量是当前项目的验收依据，不因仍有差异而无限返工。

## 先确定本轮范围与验收等级

先区分 `resume` 与 `fresh_start`。续做时沿用已选版本、数据、接受范围和有效证据，只读最新清单与未关闭问题。用户要求从新视频开始时，建立只含真实输入与来源/时间说明的允许读取清单；不读取旧 Blender/MJCF、拟合参数、动作、参考关键帧或评测结论。通用技能和已核实的硬件描述可用，旧场景先验不能冒充新观测。保留审计记录在输入范围外；不能声称清空当前对话或把同一会话伪装成盲测。

完整流程包含两步；用户只要求 Blender 建模/视频时完成第一步，只要求模型导入或诊断时在对应范围交付。输入已有合格 `.blend` 可直接进入第二步。第一步的画面认可不等于第二步的物理任务成功；用户要求走通实际物理执行时，不能停在模型加载或状态回放。

- **默认：可用重建**。主要物体、机器人连接与交互关系合理，视角和源帧对应正确，无影响任务的明显穿入、断裂或跳变。背景布局、非关键纹理、光照和细小轮廓偏差可带说明通过。
- **精确对齐**：用户要求定量几何、时序或接触精度时，按已约定指标评测任务区域。没有约定容差时，依据用途设定并记录，不把“任何非零误差”都当失败。
- **高保真/研究验证**：仅在明确需要时增加完整相机标注、精细外观、实测动力学辨识或连续检测等成本。可用重建不默认升级到此等级。

用户明确接受某版本时，记录场景哈希、接受范围、已知偏差与依据，停止追究已接受的非阻塞项。保留历史测量，不能把用户接受改写为测量误差为零；单场景的容差不能推广为通用阈值。

## 有上限的修正闭环：每场景最多 5 轮

默认 `max_iterations=5`，按**每个场景、本次明确的执行范围**计数；若同时执行两步，两步共用这 5 轮，不给每个阶段、关键帧或问题另开 5 轮。初始构建/基线检查记为第 0 轮，不计入修正次数。用户可明确调整上限；不能自行加轮。

发现本轮验收范围内的阻塞问题，且仍有修正额度与可执行方案时，**必须继续修正，不能仅记录失败、导出好看的图或提出下一步建议就结束**。每轮按以下闭环执行：

1. **定位与假设**：选当前主要阻塞项，记录受影响对象/事件区间、当前指标和修改假设。优先检查相机与相对位置，再处理接触、稳定性及外观；已接受的非阻塞偏差不重新打开。
2. **修改与计数**：保存独立候选和输入差异，修改几何、初态、控制、参数或显示设置，开始验证时占用一轮。一个被实际验证的不同修正候选算一轮；多参数联合修改需说明耦合，批量/并行测试的不同候选分别计数，不能用一轮包装无限搜索。
3. **局部与前后检查**：先跑能检验假设的短段，向异常前后扩查至稳定边界。全步几何/接触扫描与所需视角五问增量更新；新增异常纳入问题表。局部失败保留证据，拒绝候选或在下一轮换方案。
4. **回归与选择**：候选局部通过后，完成受影响范围回归；准备判定成功时，必须从最终磁盘配置做完整相关回归，核对任务、几何/数值、源时间和所需视觉输出。复验同一未修改候选不额外占一轮；回归失败则本轮失败，有额度继续下一轮。不能以牺牲原已通过任务来换取单项指标变好。
5. **轮次结论**：记录修改前后指标、覆盖范围、剩余问题以及接受/拒绝理由；保留当前最佳可解释候选与失败基线。未改善时换假设，不重复同一修改凑满次数。

**停止规则**：全部本轮必需检查通过即可提前结束，不强制跑满 5 轮；第 5 轮后仍有阻塞问题，停止新增修正，交付最佳候选及失败证据，状态写为 `iteration_limit_reached`，说明实际完成轮数、剩余问题与后续选项，不声称问题已解决或自动开始第 6 轮。只有用户停止/缩小范围，或存在缺失输入、环境不可用、无法在现有模型中检验的机制等具体阻塞，才可在额度未用尽时以 `blocked` 结束；需说明已尝试什么、为何无法继续及缺少什么。单次失败、耗时较长或需要改变诊断方法不是提前停止理由。

轮次随场景执行记录持久保存：续做、上下文压缩、切换阶段或换问题不重置计数；没有记录时从已有候选与日志恢复已用次数并注明不确定性。同配置的基础设施失败重试不是新修正轮，但须记录故障并排查，不能无限重试。纯检查或只导出已确认视频的任务不强制启动修正循环。

## 第一步：真实视频 → Blender 场景与动作

### P0—P3：建立或恢复可检查场景

1. **数据、索引与事件发现**：确认全部视角、共同帧范围、坐标/单位、时间戳与可用机器人状态。保持源帧、Blender帧、视频帧映射，区分实测、拟合、插值和模拟；播放帧率不冒充采集速率。先独立浏览真实视频，按[事件与复核规范](skills/real2sim-prompt/references/events-and-review.md)建立对象关系变化时间线，再建模；不能把仓库参考帧列表当作事件检测结果。
2. **机器人与相机**：使用当前硬件的URDF或等效结构，处理visual/collision的origin、axis、scale、limit及mimic。保留结构节点和安装件，检查FK与可见连接。固定相机使用共享外参；腕部相机通过机械链与固定手眼变换运动。以机器人运动、物体轮廓和多视角观测约束相机与物体，拟合及留出帧检查见[工程排查](skills/real2sim-prompt/references/engineering.md)。
3. **物体与场景**：先拟合操作区的尺度、形状、相对位置与材质；容器保留杯底、薄壁和空腔。显示资产与碰撞代理分开，共享对象局部坐标与运动。材质、纹理、光照和可见背景是最终视觉交付的一部分：先补对画面影响大的表面与轮廓，再按用途取舍小陈设；允许近似不等于交付基础色块。具体执行见[外观与物理轨迹渲染](skills/real2sim-prompt/references/rendering.md)。
4. **基线检查**：首次建立场景时检查全场景的完整性、支撑和操作区碰撞。已验证的资产与静态环境可复用；局部更新只使相关检查失效，不无条件重建基线。

实测关节与标定不得为视觉对齐被无记录改写。工具抓取偏差先查安装变换、URDF导入、工具尺度和物体姿态。碰到具体结构/接触问题时才读[工程排查与增量验证](skills/real2sim-prompt/references/engineering.md)。

### P4：交互关键帧与五问

执行本阶段先读[事件驱动关键帧、邻域五问与物理判定](skills/real2sim-prompt/references/events-and-review.md)。关键帧来自逻辑对象之间**接触、抓持、支撑、包含等关系的建立、解除或改变**。每个事件覆盖 `before / onset（或不确定区间）/ after`；起止、仓库参考、均匀采样帧只作额外上下文，不能替代事件覆盖。

真实与模拟事件分别记录，取两者邻域与异常窗口的并集；不强行配对不同时刻的首触。每个检查帧、每个可用视角都回答：**① 最大差异；② 同帧相机对齐；③ 物体相对位置；④ 穿模与接触；⑤ 逼真度（材质、纹理、灯光、背景）**。先核对源时间/相机、相对位置，再修接触和外观。所有新增检查帧都保留五问证据，遮挡未知不能自动通过。

首次逐帧建立观察；续做只更新受影响项，未失效的共享证据可以引用，未检查的新帧不能用邻帧结论代填。①是观察项；其余按当前验收等级使用 `pass / pass_with_notes / fail / review / not_evaluated`。用户接受的非阻塞偏差不反复否决。完整覆盖表与报告由结构化记录生成，避免重复撰写不变解释。

### 修改与回归：范围由影响决定

- 相机/源索引或重要相对位置有阻塞错误时优先修正，再处理接触和外观。针对本轮最大阻塞项修改，保留可回退版本；不顺手重做无关背景和动力学。
- 从出问题关键帧向前、向后检查，直到异常区间两端稳定。几何问题检查区间内全部输出帧，并在快速接触、薄壁和抓取/释放边界加密；不只修一张图，也不默认全片最细步长。
- 先让候选通过局部几何和过渡检查，再做受影响的物料模拟和最终渲染。保持四元数连续、接触约束及支撑；不要用逐帧大幅投影修正制造跳变。
- 依赖发生改变才重算：材质不使FK失效；局部杯轨迹不使静态柜体检查失效；相机变化重查相关全部投影；杯轨迹/几何变化使相关物料模拟失效。局部通过后对完整输出序列运行一次相关回归，已有缓存可复用。
- 同一种修正连续两轮没有改善时，换诊断方法或缩小自由度，并记录限制；继续遵守共享的 5 轮上限，不能仅因两轮未改善就提前交付失败。达到本轮等级且相关回归无新增阻塞问题即可停止；用户已接受的偏差不再循环修。

不能以隐藏对象、删除失败帧、独立平移视频或事后扩大容差制造通过。结果可以在既定容差内通过，不能宣称严格零相交或力学真实性已经证明。

### P5—P6：按需导出与验收

最终 real/sim 视觉交付默认使用 Blender 的完整显示场景。先按[外观与物理轨迹渲染](skills/real2sim-prompt/references/rendering.md)补材质、纹理、灯光与主要背景，再完成代表时刻的全部视角样帧检查。已有用户接受的外观直接复用；仅模型加载/接触诊断可用基础渲染，但必须标明用途。

用户只要关键帧时不渲染整段；请求视频时直接导出当前已确认版本，并披露保留偏差，不为次要外观重新打开整个建模任务。全部可用视角均交付，real/sim始终保持同源索引；不为两视角数据虚构第三视角。用户需要当前未解决版本的诊断视频时也可以导出，状态应写清楚。

冻结工程与渲染配置哈希，复用同版本帧并补齐缺帧。编码后完整解码核对数量、尺寸、播放时间，抽查首末帧及抓取/落料/释放事件与源图对应；视频成功和几何/时序精度分别记录。导出阶段不重跑未失效的建模和五问基线。

交付简要说明改了什么、通过哪一级、还有哪些不影响本次交付的差异，并给直接文件链接。只有记录结构需要创建/调整时读[输出约定](skills/real2sim-prompt/references/output-contract.md)。

## 第二步：Blender → MuJoCo 场景与实际物理执行

执行本步读[迁移与接触校准](skills/real2sim-prompt/references/mujoco.md)和[事件与物理判定](skills/real2sim-prompt/references/events-and-review.md)，沿用第一步工程与接受范围。流程是：固定输入与短性能检查 → 转移结构/空腔并核对同源状态 → 分阶段物理执行 → 对准、闭合、双侧承载、力与摩擦校准 → 最终完整回归 → 将本次原生轨迹回传 Blender 显示场景 → 按需精细渲染。

模型导入、运动学回放、基础接触、实际任务及几何验收分别记录。物体由驱动器、接触与重力推进；状态回放或抓持附着不能冒充原生任务成功。保持所有可用视角和源时间映射，五问仅增量更新受影响项。任务完成不覆盖未解决的穿入问题，第一步画面认可也不代表物理通过。

运行慢、物料/接触密集、需要恢复长跑或优化时，再读[性能与增量执行](skills/real2sim-prompt/references/mujoco-performance.md)。先短段定位成本和失败，候选有效后再跑全程；不要默认渲染每个失败候选或固定追加扰动批次。

## 控制时间与tokens

主入口只读一次；按需加载参考，优先读取摘要、问题区间和配置差异。数值扫描、候选搜索、统计、拼图、表格与视频核验交给可复用脚本；工具只返回计数、极值、失败区间和少量证据。模型集中处理新图像的空间歧义与方案选择。先看多视角总览，问题区域再放大；不要为每次微调重新加载全片图像和大段日志。

事件覆盖、五问证据和物理结果分别计数；未做的检查不能由参考图或退出码补成通过。第二步先做无渲染的数值诊断和少量分阶段候选，再渲染合格模型；只保存关键帧和异常窗口的详细接触量，全程扫描返回极值、区间和实际分母。参考脚本先检查适用性；不把场景专用导出器包装成未经验证的通用转换器。输出配置变化后更新相应哈希与依赖，保留失败基线供对照。
